# ================================
# GuardOS — Example Environment
# ================================
# Copy this file to:  ~/.guardos/ai.env  (or project root as .env)
# Adjust values to your local setup. This file contains NO secrets.

# --- AI MODE & RUNTIME -----------------------------------------------
# GuardOS is local-first. Hybrid/Cloud require Guard‑Tunnel (future).
GUARDOS_AI_MODE=local            # local | hybrid | cloud

# Primary local runtime executable (installed by the user, not vendored here).
# Example values: llama.cpp main binary name on your system
GUARDOS_AI_RUNTIME=llama.cpp

# --- MODEL SELECTION --------------------------------------------------
# Choose a model ID defined in ai/models/registry.yaml
GUARDOS_MODEL_ID=gemma-0.27b-q4km

# Absolute path to your local model file (GGUF). Do not commit real paths.
# Example:
#   ~/.guardos/models/gemma-270m/gemma-0.27b-Q4_K_M.gguf
GUARDOS_MODEL_PATH=/home/USER/.guardos/models/gemma-270m/gemma-0.27b-Q4_K_M.gguf

# --- GENERATION DEFAULTS ----------------------------------------------
GUARDOS_MAX_TOKENS=256
GUARDOS_TEMP=0.7
GUARDOS_TOP_P=0.95

# --- POLICY ENFORCEMENT -----------------------------------------------
# Paths to policy files (kept in repo). Runners/adapters must respect these.
GUARDOS_POLICY_SAFETY=ai/policies/safety.yaml
GUARDOS_POLICY_TOOLS=ai/policies/tool_rules.yaml

# --- PROMPTS -----------------------------------------------------------
# Base prompt(s) for GuardPanel or other agents.
GUARDOS_PROMPT_GUARDPANEL=ai/prompts/guardpanel/base.md

# --- LOGGING -----------------------------------------------------------
# Local-only logs. No uploads. Runners should sanitize PII and rotate logs per policy.
GUARDOS_LOG_DIR=.guardos/logs
GUARDOS_LOG_ROTATE_DAYS=14
GUARDOS_LOG_LEVEL=info          # error | warn | info | debug

# --- PROFILES ----------------------------------------------------------
# Active profile file (YAML) describing OS/security defaults for this run.
# Example: profiles/minimal.yaml
GUARDOS_PROFILE=profiles/minimal.yaml

# --- CONSENT / INTERACTION --------------------------------------------
# If true, tools like run_command must ask explicit yes/no before executing.
GUARDOS_REQUIRE_CONSENT=true

# --- FUTURE: GUARD‑TUNNEL (PLACEHOLDERS) ------------------------------
# These are placeholders for future encrypted connectivity. Leave as-is.
GUARDOS_TUNNEL_ENABLED=false
GUARDOS_TUNNEL_ENDPOINT=
GUARDOS_TUNNEL_DEVICE_KEY_PATH=
